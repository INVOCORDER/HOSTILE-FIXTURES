# Tampered signed bundle

Fixture: `tampered-signed-bundle`

Expected result:

- valid: `false`
- errors: `bundle sha256 mismatch, signature verification failed`

This fixture concerns signature envelope verification only. It does not prove truth, safety, authorization, admissibility, or legitimacy.
